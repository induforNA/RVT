<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sayone.omidyar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.RegistrationActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.AboutActivity" />
        <activity android:name=".view.LandTypeSelectionActivity" />
        <activity android:name=".view.AdminLoginActivity" />
        <activity android:name=".view.AdminDashBoard" />
        <activity android:name=".view.FlashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>